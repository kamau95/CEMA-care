<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Enroll a Client in Programs - Health Program Management</title>
  <link rel="stylesheet" href="/css/enroll-client.css"
</head>
<body>
  <div class="container">
    <!-- Sidebar -->
    <aside class="sidebar">
      <h1 class="sidebar-title">Health Program</h1>
      <nav>
        <ul>
          <li class="sidebar-item">
            <a href="create-program" class="sidebar-link">Create a Health Program</a>
          </li>
          <li class="sidebar-item">
            <a href="register-client" class="sidebar-link">Register a New Client</a>
          </li>
          <li class="sidebar-item active">
            <a href="#" class="sidebar-link">Enroll a Client in Programs</a>
          </li>
          <li class="sidebar-item">
            <a href="search-client" class="sidebar-link">Search for a Client</a>
          </li>
          <li class="sidebar-item">
            <a href="search-client" class="sidebar-link">View Client Profile</a>
          </li>
        </ul>
      </nav>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
      <div class="form-container">
        <h2 class="form-title">Enroll a Client in Programs</h2>
        <div class="form">
          <div class="form-group">
            <label for="client-search" class="form-label">Search Client</label>
            <input
              type="text"
              id="client-search"
              class="form-input"
              list="client-list"
              placeholder="e.g., John Doe"
              required
            >
            <datalist id="client-list">
              <!-- Options to be populated dynamically via JavaScript -->
            </datalist>
          </div>
          <div class="form-group">
            <label for="program-select" class="form-label">Select Program</label>
            <select
              id="program-select"
              class="form-select"
              required
            >
              <option value="">Select Program</option>
              <!-- Options to be populated dynamically via JavaScript -->
            </select>
          </div>
          <button id="enroll-client-btn" class="form-button">Enroll Client</button>
        </div>
      </div>
    </main>
  </div>
</body>
</html>